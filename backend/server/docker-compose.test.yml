# Override for testing - uses tmpfs instead of persistent volume
# to avoid permission issues in GitHub Actions
services:
  postgres:
    volumes: []  # Clear the volume mount
    tmpfs:
      - /var/lib/postgresql/data:rw,noexec,nosuid,size=1g
